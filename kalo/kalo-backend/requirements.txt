# ============================================================================
# KALO BACKEND - Production Requirements
# ============================================================================
# Install with: pip install -r requirements.txt
# Update with: pip install --upgrade -r requirements.txt
# ============================================================================

# WEB FRAMEWORK
# ============================================================================
fastapi==0.104.1                  # Modern async web framework
uvicorn==0.24.0                   # ASGI server
python-multipart==0.0.6           # Multipart form parsing

# ENVIRONMENT & CONFIG
# ============================================================================
python-dotenv==1.0.0              # Load .env files
pydantic==2.5.0                   # Data validation
pydantic-settings==2.1.0          # Settings management

# ASYNC & NETWORKING
# ============================================================================
aiohttp==3.9.1                    # Async HTTP client
httpx==0.25.2                     # Modern HTTP client
requests==2.31.0                  # HTTP requests library

# BACKGROUND JOBS & MESSAGE QUEUE
# ============================================================================
celery==5.3.4                     # Distributed task queue
redis==5.0.1                      # Redis client for broker/cache
kombu==5.3.2                      # Celery message broker

# DATABASE
# ============================================================================
sqlalchemy==2.0.23                # ORM
psycopg2-binary==2.9.9            # PostgreSQL driver
alembic==1.13.0                   # Database migrations

# AUTHENTICATION
# ============================================================================
python-jose==3.3.0                # JOSE tokens
passlib==1.7.4                    # Password hashing
bcrypt==4.1.1                     # Password hashing
cryptography==41.0.7              # Cryptography

# RECIPE EXTRACTION - VIDEO PROCESSING
# ============================================================================
yt-dlp==2023.12.30                # Download videos (TikTok, Instagram, YouTube)
opencv-python==4.8.1.78           # Video/image processing
imageio==2.33.0                   # Image I/O and processing
imageio-ffmpeg==0.4.9             # FFmpeg for imageio

# RECIPE EXTRACTION - AUDIO PROCESSING
# ============================================================================
openai-whisper==20231214          # Audio transcription (OpenAI)
librosa==0.10.0                   # Audio feature extraction

# RECIPE EXTRACTION - OCR (Text Detection)
# ============================================================================
paddleocr==2.7.0.2                # Optical character recognition
paddlepaddle==2.5.1               # PaddlePaddle ML framework

# RECIPE EXTRACTION - COMPUTER VISION (Object Detection)
# ============================================================================
ultralytics==8.0.227              # YOLOv8 object detection
torch==2.1.1                      # PyTorch (dependency for YOLOv8)
torchvision==0.16.1               # Computer vision (dependency for YOLOv8)

# AI/LLM INTEGRATION
# ============================================================================
openai==1.3.9                     # OpenAI API (GPT-3.5, GPT-4)
anthropic==0.7.10                 # Anthropic Claude API (optional)

# AWS/CLOUD
# ============================================================================
boto3==1.34.5                     # AWS SDK
botocore==1.34.5                  # AWS Core

# UTILITIES
# ============================================================================
python-dateutil==2.8.2            # Date/time utilities
pytz==2023.3                       # Timezone support

# LOGGING & MONITORING
# ============================================================================
python-json-logger==2.0.7         # JSON logging
loguru==0.7.0                     # Enhanced logging

# DEVELOPMENT & TESTING
# ============================================================================
pytest==7.4.3                     # Testing framework
pytest-asyncio==0.21.1            # Pytest async support
black==23.12.0                    # Code formatting
flake8==6.1.0                     # Linting
mypy==1.7.1                       # Type checking

# ============================================================================
# SYSTEM DEPENDENCIES (must be installed separately):
# ============================================================================
# - ffmpeg: For video/audio processing
#   macOS: brew install ffmpeg
#   Ubuntu: sudo apt-get install ffmpeg
#   Windows: choco install ffmpeg
# ============================================================================

# NOTES:
# - ML Models will be auto-downloaded on first use:
#   * Whisper model (openai-whisper)
#   * YOLOv8 weights (ultralytics)
#   * PaddleOCR models (paddleocr)
# - GPU support requires CUDA-compatible PyTorch
# - For production CPU-only: pip install torch==2.1.1 --index-url https://download.pytorch.org/whl/cpu
# ============================================================================

# Utilities
python-dotenv==1.0.0
pydantic-extra-types==2.3.0

# Logging
python-json-logger==2.0.7

# Development
pytest==7.4.3
pytest-asyncio==0.21.1
black==23.12.0
flake8==6.1.0
mypy==1.7.1

# Production
gunicorn==21.2.0
python-multipart==0.0.6
